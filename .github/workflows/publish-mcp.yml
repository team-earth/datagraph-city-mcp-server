name: Publish MCP Server

on:
  push:
    tags:
      - "v*" # Triggers on tags like v1.0.0, v1.1.0, etc.

permissions:
  contents: write # For creating GitHub releases
  id-token: write # For MCP Registry OIDC authentication

jobs:
  publish:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'
      
      - name: Get version from tag
        id: get_version
        run: |
          # Remove 'v' prefix from tag (e.g., v1.0.0 -> 1.0.0)
          VERSION=${GITHUB_REF#refs/tags/v}
          echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
          echo "Publishing version: $VERSION"
      
      - name: Verify package.json version matches tag
        run: |
          PACKAGE_VERSION=$(node -p "require('./package.json').version")
          TAG_VERSION="${{ steps.get_version.outputs.VERSION }}"
          
          if [ "$PACKAGE_VERSION" != "$TAG_VERSION" ]; then
            echo "❌ Error: package.json version ($PACKAGE_VERSION) doesn't match tag ($TAG_VERSION)"
            exit 1
          fi
          
          echo "✅ Version $PACKAGE_VERSION matches tag"
      
      - name: Install dependencies
        run: npm ci || npm install
      
      - name: Run tests (if any)
        run: npm test || echo "No tests defined, skipping..."
        continue-on-error: true
      
      - name: Publish to NPM
        run: npm publish --access public --provenance || echo "Package version may already exist, continuing..."
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
      
      - name: Install MCP Publisher CLI
        run: |
          curl -L "https://github.com/modelcontextprotocol/registry/releases/latest/download/mcp-publisher_$(uname -s | tr '[:upper:]' '[:lower:]')_$(uname -m | sed 's/x86_64/amd64/;s/aarch64/arm64/').tar.gz" | tar xz mcp-publisher
          chmod +x mcp-publisher

      - name: Login to MCP Registry (GitHub OIDC)
        run: ./mcp-publisher login github-oidc

      - name: Publish to MCP Registry
        run: ./mcp-publisher publish
      
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ steps.get_version.outputs.VERSION }}
          name: Release v${{ steps.get_version.outputs.VERSION }}
          body: |
            ## DataGraph MCP Server v${{ steps.get_version.outputs.VERSION }}
            
            ### Installation
            ```bash
            npx datagraph-mcp-server
            ```
            
            ### What's Changed
            See [CHANGELOG.md](https://github.com/team-earth/datagraph-city-mcp-server/blob/main/CHANGELOG.md)
            
            ### Links
            - 📦 [NPM Package](https://www.npmjs.com/package/datagraph-mcp-server)
            - 🌐 [MCP Registry](https://registry.modelcontextprotocol.io)
            - 📖 [Documentation](https://github.com/team-earth/datagraph-city-mcp-server#readme)
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Notify completion
        run: |
          echo "✅ Successfully published v${{ steps.get_version.outputs.VERSION }}"
          echo "📦 NPM: https://www.npmjs.com/package/datagraph-mcp-server"
          echo "🐙 GitHub: https://github.com/team-earth/datagraph-city-mcp-server/releases"

